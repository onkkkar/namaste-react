# Regenerable via `npm i` - never push node_modules to Git
/node_modules

# Parcel's build output - regenerated by `npx parcel build index.html`
/dist

# Parcel's build cache - regenerated automatically, safe to delete anytime
.parcel-cache
